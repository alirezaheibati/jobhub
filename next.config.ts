import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  basePath: "/projects/jobhob",
  images: {
    domains: ["alirezaheibati.ir"], // Add the domain hosting the image
  },
};

export default nextConfig;
